import { it } from "vitest";

// TO DO - Priscilla's ticket P-13

it.todo("returns the full catalog");

it.todo("filters the catalog by category");

it.todo("rejects creating a tool without a token");

it.todo("rejects editing a tool you do not own");

// Extras

// P-04
it.todo("filters the catalog to available tools only");

// P-04
it.todo("applies the category and availability filters together");

// P-09 (409)
it.todo("rejects deleting a tool that is currently borrowed");