import { it } from "vitest";

// TO DO - Nyisha's ticket N-09

it.todo("creates a new user and returns a token");

it.todo("rejects a second account with the same email");

it.todo("rejects a login with the wrong password");

it.todo("rejects /auth/me when no token is sent");

// Extra - from our CONTRACT.md. Add if there is time.
// Nyisha's TO DO in CONTRACT.md N-04

it.todo("rejects a register with a missing field");

it.todo("rejects a password shorter than 8 characters");
